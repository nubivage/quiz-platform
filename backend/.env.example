# Server
PORT=3000
CORS_ORIGIN=http://localhost:5173

# Database driver: "sqlite" for local dev, "dynamo" for AWS
DB_DRIVER=sqlite

# Room rules
MAX_PLAYERS_PER_ROOM=150

# Only read when DB_DRIVER=dynamo
AWS_REGION=us-east-1
DYNAMO_ROOMS_TABLE=Rooms
DYNAMO_RESULTS_TABLE=QuizResults

